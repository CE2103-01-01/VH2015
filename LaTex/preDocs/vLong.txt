Clase vLong


Ámbito privado:

vRef data: referencia en vHeap al dato que contiene la clase



Ámbito publico:

vLong();
Proceso realizado:
Construye la clase con valor por defecto en 0.

vLong(long);
Parámetros:
long: variable a guardar en vHeap.
Proceso realizado:
Hace un vMalloc para crear la referencia data y un vPlacement para llenarla con el parámetro.

~vLong();
Proceso realizado:
Destruye la referencia al dato en vHeap.

int operator =(long);
Parámetros:
long: variable a la que se está igualando.
Proceso realizado:
Asigna el parametro al valor almacenado en la vRef.

int operator =(vLong);
Parámetros:
vLong: variable a la que se está igualando.
Proceso realizado:
Asigna el parametro al valor almacenado en la vRef. Retorna 0 si todo sale bien.

long operator !();
Proceso realizado:
Des referencia el dato contenido el la vRef y lo retorna.

int operator +=(long);
Parámetros:
long: variable que se esta sumando
Proceso realizado:
Suma el parámetro al dato apuntado por la vRef. Retorna 0 si todo sale bien.

vRef operator &();
Proceso realizado:
Retorna la vRef corresondiente al dato almacenado.

int operator --();
Proceso realizado:
Resta 1 a la variable apuntada por la vRef. Retorna 0 si todo sale bien.

int operator ++();
Proceso realizado:
Suma 1 a la variable apuntada por la vRef. Retorna 0 si todo sale bien.

bool operator <(vLong);
Parámetros:
vLong: variable con la cual se va a comparar
Proceso realizado:
Compara los datos de las vRef. Retorna true si el parámetro es mayor.

bool operator <=(vLong);
Parámetros:
vLong: variable con la cual se va a comparar
Proceso realizado:
Compara los datos de las vRef. Retorna true si el parámetro es mayor o igual.

bool operator >(vLong);
Parámetros:
vLong: variable con la cual se va a comparar
Proceso realizado:
Compara los datos de las vRef. Retorna true si el parámetro es menor.

bool operator >=(vLong);
Parámetros:
vLong: variable con la cual se va a comparar
Proceso realizado:
Compara los datos de las vRef. Retorna true si el parámetro es menor o igual.

bool operator ==(vLong);
Parámetros:
vLong: variable con la cual se va a comparar
Proceso realizado:
Compara los datos de las vRef. Retorna true si el parámetro es igual.

bool operator !=(vLong);
Parámetros:
vLong: variable con la cual se va a comparar
Proceso realizado:
Compara los datos de las vRef. Retorna true si el parámetro es diferente.

bool operator <(long);
Parámetros:
long: variable con la cual se va a comparar
Proceso realizado:
Compara el dato de la vRef con el parámetro. Retorna true si el parámetro es mayor.

bool operator <=(long);
Parámetros:
long: variable con la cual se va a comparar
Proceso realizado:
Compara el dato de la vRef con el parámetro. Retorna true si el parámetro es mayor o igual.

bool operator >(long);
Parámetros:
long: variable con la cual se va a comparar
Proceso realizado:
Compara el dato de la vRef con el parámetro. Retorna true si el parámetro es menor.

bool operator >=(long);
Parámetros:
long: variable con la cual se va a comparar
Proceso realizado:
Compara el dato de la vRef con el parámetro. Retorna true si el parámetro es menor o igual.

bool operator ==(long);
Parámetros:
long: variable con la cual se va a comparar
Proceso realizado:
Compara el dato de la vRef con el parámetro. Retorna true si el parámetro es igual.

bool operator !=(long);
Parámetros:
long: variable con la cual se va a comparar
Proceso realizado:
Compara el dato de la vRef con el parámetro. Retorna true si el parámetro es diferente.